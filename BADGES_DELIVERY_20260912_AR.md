# تسليم نظام الأوسمة والألقاب — SAKI

تم تنفيذ نظام أوسمة حقيقي داخل التطبيق وقاعدة Supabase، مع صور PNG مولدة خصيصًا وحفظها داخل `assets/badges/`. تظهر الأوسمة في قسم **الأوسمة** داخل صفحة بروفايل المستخدم التي تُفتح عند الضغط على صورة المستخدم من اللحظات والريلز، لأن صفحة البروفايل الحالية تجلب `userBadges` وتعرض صور `asset_path` المحلية.

## الأوسمة المضافة

| المجموعة | القيم |
|---|---|
| إرسال الهدايا | 1M، 10M، 50M، 100M عملة ذهبية |
| أول شحنة | أول شحن موثق منفرد بقيمة 7,500 عملة ذهبية أو أكثر |
| إجمالي الشحن | 1M، 10M، 50M، 100M عملة ذهبية |
| مالك الغرفة | هدايا غرفتك بقيمة 1M، 10M، 50M، 100M |
| الألقاب | راعي الهدايا، سيد الشحن، مالك الغرفة، الأسطورة النخبة |

كل وسام يُمنح مرة واحدة فقط عبر `user_badges`. وعند استحقاقه تُنشأ تلقائيًا صفّة في `notifications` بنوع `badge_earned` مع `badge_key` و`badge_asset_path` وبيانات الوسام؛ لذلك يظهر في إشعارات النظام وليس محليًا فقط.

## الربط الخلفي

تم توسيع `badge_catalog` و`evaluate_user_badges` في Supabase. التقييم يعتمد على جداول المعاملات الحقيقية: `room_gifts` للهدايا، و`shipping_transactions` للشحن من وكيل الشحن، و`rooms` لحساب إنجازات مالك الغرفة. أضيفت محفزات PostgreSQL بعد إدخال الهدية أو معاملة الشحن، مع إعادة تقييم الحسابات الموجودة مرة واحدة بعد migration.

أضيف جدول `verified_gold_recharge_events` للشحن الموثق من Google Play أو أي مزود دفع موثوق. لا تُحتسب صفوفه إلا عند `verified=true`، ولا يستطيع تطبيق Flutter منح وسام شحن لنفسه. تكامل Google Play النهائي يحتاج خادمًا موثوقًا يتحقق من إيصال Google Play ثم يسجل الحدث الموثق؛ هذا يحمي الأوسمة من التزوير.

## التحقق

نجح `flutter analyze` بدون أخطاء. تم بناء APK Release وتحقق توقيعه عبر APK Signature Scheme v2. تم دفع التعديل إلى GitHub في commit `d691992` على فرع `main`.

## ملاحظة حجم APK

صور الأوسمة الجديدة ضُغطت إلى 768px مع الحفاظ على الشفافية. حجم APK النهائي كبير لأن المشروع يحتوي مسبقًا على عدد كبير من أصول الصور والألعاب، وليس بسبب منطق الأوسمة وحده.
