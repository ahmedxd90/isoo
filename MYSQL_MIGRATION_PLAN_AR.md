# خطة ترحيل SAKI إلى MySQL/MariaDB

## الحالة الحالية

تم التحقق من مشروع Supabase `saki` والمخطط العام. يحتوي المخطط على **93 جدولًا**، مع بيانات فعلية في الملفات الشخصية والمنشورات والغرف والرسائل والهدايا والإشعارات والعائلات والمهام والمحافظ. قاعدة MariaDB على cPanel متصلة بنجاح عبر `health.php`.

## قواعد التنفيذ

لن يتم حذف Supabase أثناء الترحيل. سيبقى مصدرًا احتياطيًا حتى يتم تشغيل اختبارات المصادقة والبيانات والرفع والرسائل على MariaDB بنجاح. يجب أن يحافظ الترحيل على UUIDs والمعرفات الرقمية والعلاقات بين الجداول حتى لا تتغير روابط المنشورات والرسائل والهدايا.

## طبقات التطبيق المطلوبة

| الطبقة | الوظيفة | الحالة |
|---|---|---|
| الاتصال | PDO وMariaDB وUTF-8 وقيود المهلة | يعمل على الخادم |
| المصادقة | register/login/logout/session وpassword hashing | نسخة أولية تعمل |
| الملف الشخصي | القراءة والتحديث ورفع الصورة | نسخة أولية تعمل |
| البيانات الاجتماعية | profiles/posts/reels/follows/comments/likes/shares | تم إنشاء مخطط MariaDB؛ الترحيل والربط مع Flutter قيد الاختبار |
| الرسائل | conversations/messages/reactions/unread | قيد التنفيذ |
| الغرف | rooms/members/seats/moderators/bans/mutes | قائمة الغرف أولية فقط |
| الاقتصاد | gifts/wallets/recharges/store/vip | قيد التنفيذ |
| الإدارة | roles/audit/bans/reports | قيد التنفيذ |
| Google OAuth | التحقق من Google ID Token وربط الحساب | يحتاج Client IDs |
| Flutter | استبدال SupabaseClient بعميل REST تدريجيًا | لم يبدأ التحويل الكامل |

## ملاحظات مهمة

تسجيل Google يحتاج Web Client ID وAndroid Client ID وبصمة SHA-1 للتوقيع. كلمات مرور Supabase لا يمكن نقلها بصورتها الأصلية؛ الحسابات القديمة تحتاج إعادة تعيين كلمة المرور أو مسار تسجيل دخول انتقالي.

لا يجوز وضع بيانات MySQL داخل APK. جميع عمليات قاعدة البيانات يجب أن تمر عبر API HTTPS مع جلسات قصيرة/قابلة للإلغاء وصلاحيات على الخادم.

## التحقق

قبل تحويل كل ميزة في Flutter، يجب اختبار endpoint الخاص بها باستجابة نجاح وفشل، ثم تشغيل `flutter analyze` وبناء APK. أي فشل يعيد الميزة إلى Supabase مؤقتًا بدل كسر التطبيق كله.
