# SAKI Role-Based Access Control

تم تنفيذ نظام صلاحيات قائم على قاعدة البيانات في Supabase، وليس على إخفاء عناصر الواجهة فقط. كل دالة إدارية تعيد التحقق من الدور والصلاحية عند التنفيذ.

## الأدوار

| الدور | الوصف | الصلاحيات الأساسية |
|---|---|---|
| `super_admin` | الإدارة العليا | جميع الصلاحيات، بما فيها إدارة الأدوار، VIP، العملات، النظام، الحظر، المحتوى، التقارير، الوكالات والعائلات |
| `admin` | الإدارة التشغيلية | مشاهدة المستخدمين، إجراءات المستخدمين التشغيلية، الحظر، الغرف، البلاغات، المحتوى، والتحليلات |
| `bd` | Business Development | الوكالات، العائلات، الشراكات، متابعة المستخدمين والتحليلات التجارية |
| `official_host` | مذيع SAKI الرسمي | الغرف الرسمية، البث الرسمي، مشاهدة المستخدمين والتحليلات المرتبطة بالبث |
| `customer_service` | خدمة العملاء | مشاهدة المستخدمين، دعم الحالات، البلاغات، والتحليلات المحدودة |
| `user` | مستخدم عادي | لا توجد صلاحيات إدارية |

## الصلاحيات الحساسة

تظل إدارة الأدوار، Super Admin، الأرصدة والعملات، VIP، SAKI ID، إعدادات النظام، وسجل التدقيق محصورة في `super_admin`. لا يمكن لأي دور تخفيض صلاحية نفسه من خلال الدالة الإدارية.

## الحماية الخادمية

أضيف جدول `admin_role_permissions` لمصفوفة الدور والصلاحية، وجدول `admin_audit_log` لتسجيل الإجراءات الإدارية. الدوال `saki_current_admin_role` و`saki_has_admin_permission` تعيدان حساب الدور من `profiles` عند كل استدعاء. كما تم تقييد الدوال الإدارية صراحةً عن `anon`، ومنح تنفيذها للمستخدمين المسجلين فقط مع إعادة التحقق الداخلي.

تم تحديث سياسات RLS للوكالات والعائلات والبلاغات لتطابق الصلاحيات الجديدة، مع بقاء بيانات التطبيق ووظائفه الحالية دون حذف أو إعادة بناء.

## الواجهة

أضيفت لوحة موحدة للأدوار التشغيلية، وتظهر للمستخدم من صفحة Profile حسب الدور الفعلي. تتضمن اللوحة مؤشرات، المستخدمين، الغرف والبث الرسمي، الوكالات، العائلات، البلاغات، وسجل التدقيق عندما تسمح الصلاحية.

أضيف مكوّن شارة متجهة خفيفة للأدوار يعرض `ADMIN` و`BD` و`OFFICIAL` و`SUPPORT` و`SUPER ADMIN`، ولا يخلط هذه الأدوار مع مستويات VIP من 1 إلى 10.

## التحقق

تم التحقق من أن دوال الإدارة لا تسمح بتنفيذ `anon`، وأن `flutter analyze` ينتهي بلا أخطاء، وأن APK Release يمر بفحص التوقيع وسلامة الأرشيف.
